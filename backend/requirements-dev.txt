# ─── MediSphere AI — Dev/Demo Requirements ──────────────────────────────────
# Lightweight install for local demo (no PostgreSQL, Redis, or GPU required)
# Uses in-memory mock data — no database connection needed

# FastAPI & Server
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# Auth & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.2.2

# Validation
pydantic==2.8.2
pydantic-settings==2.3.4
email-validator==2.2.0

# Utilities
python-dateutil==2.9.0
faker==26.0.0
rich==13.7.1
loguru==0.7.2
httpx==0.27.0

# Scheduling (for agent loops)
apscheduler==3.10.4
